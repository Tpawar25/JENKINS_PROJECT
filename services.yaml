apiVersion: v1
kind: Service
metadata:
  name: indiaproj-service
spec:
  selector:
    app.kubernetes.io/name: indiaproj
  ports:
    - protocol: TCP
      port: 87
      targetPort: 8080
  type: NodePort
